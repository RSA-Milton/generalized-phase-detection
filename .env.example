# GPD Data Directory Configuration
# Copy this file to .env and modify paths according to your setup

# Main data directory (external to repository)
GPD_DATA_DIR=/path/to/external/gpd_data

# Subdirectories for organized data storage
GPD_RAW_DIR=${GPD_DATA_DIR}/raw
GPD_PREPROCESSED_DIR=${GPD_DATA_DIR}/preprocessed
GPD_RESULTS_DIR=${GPD_DATA_DIR}/results
GPD_MODELS_DIR=${GPD_DATA_DIR}/models

# Optional: Temporary processing directory
GPD_TEMP_DIR=${GPD_DATA_DIR}/temp

# Example paths:
# GPD_DATA_DIR=/home/user/seismic_data/gpd
# GPD_DATA_DIR=/mnt/storage/research/gpd_data
# GPD_DATA_DIR=D:/Research/GPD/data